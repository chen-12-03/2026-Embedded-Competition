# 2026-Embedded-Competition

This repository now focuses on the embedded competition project under `projects/embedded_dev_new`.

## Layout

- `projects/embedded_dev_new/` - embedded vision system project, notes, docs, tests, and assets
- `.gitignore` - root-level ignore rules for temporary files and local artifacts

## Notes

The repository history was cleaned so the remote `main` branch keeps only this project content.
